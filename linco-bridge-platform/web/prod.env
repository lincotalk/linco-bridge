# 官方在线 Demo 生产构建配置（npm run build:h5 / build:mp-weixin 自动加载）
# 仅用于部署 bridge-demo.lincotalk.com；二次开发/自托管请改成本地 server 或自有域名，勿指向官方 Demo API
# 与 API 同域部署时可留空，走相对路径 /api
VITE_API_BASE_URL=https://bridge-demo.lincotalk.com
VITE_USE_REMOTE_API=true
